// // we will create a function which will remove the negative values from the array
// function removeNegatives() {
//     // we have an array composed with negatives and positives values
//     var arr = [3, 7, -23, 0, 2.5, -4];
//     // we created a 2nd array where we will put in it only the positive  values
//     var arrPositives=[];
//     // we willcheck in this phase if the values are positive or negative
//     for(var i=0; i<=arr.length-1; i++) {
//         if(arr[i] >= 0) {
//             // if it's positive we will add it to the new array
//             arrPositives=arr[i];
//         }
//     }
//     console.log(arrPositives);
//     return arrPositives;
// }

// removeNegatives()