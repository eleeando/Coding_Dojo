// likeCount variable represents the number of likes
var likeCount = 0;
// the function increaseLikes will count how much the variable likeCount will increase
function increaseLikes() {
    // this operation means that we will add 1 to the old value of the variable likeCount
    likeCount = likeCount + 1;
}